#include "node.hh"

namespace gad
{
    Node::Node(T* data)
    {
    }
    size_t Node::get_id()
    {
    }
    std::vector<Edge> Node::get_edges()
    {
    }
    std::vector<Node> Node::get_adjency()
    {
    }
}